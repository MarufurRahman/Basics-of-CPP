#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    cout << fmax(10, 11) << endl; // Find the big number
    cout << fmin(10, 11) << endl; // Find the small number
    cout << pow(5, 2) << endl; // Do power of them
    return 0;
}
